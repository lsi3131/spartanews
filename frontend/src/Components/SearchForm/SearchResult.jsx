import React from 'react'

function SearchResult({ results }) {
    return (
        <div>
            {/* {results.map((result) => (
                <div key={result.id}>
                    <h2>{result.title}</h2>
                    <p>{result.content}</p>
                </div>
            ))} */}
        </div>
    )
}

export default SearchResult
